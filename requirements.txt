# LowaCode AI Tutor — Python dependencies
#
# NOTE: onnxruntime is intentionally NOT pinned here. install_linux.sh selects the
# hardware-appropriate variant (onnxruntime-gpu / -rocm / -openvino / plain CPU)
# after this file is installed. kokoro-onnx pulls plain onnxruntime as a fallback.

# --- Web layer (acolbert.py, scripts/orchestrator_server.py) ---
fastapi==0.139.0
starlette==1.3.1
uvicorn==0.51.0
uvloop==0.22.1          # optional asyncio speedup uvicorn uses if present
python-multipart==0.0.32  # required by the UploadFile/File endpoints
h11==0.16.0
click==8.4.2
anyio==4.14.1
sniffio==1.3.1

# --- Data models ---
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.7.0
typing_extensions==4.16.0
typing-inspection==0.4.2

# --- HTTP clients ---
requests==2.34.2
urllib3==2.7.0
certifi
charset-normalizer==3.4.9
idna==3.18
httpx==0.28.1
httpcore==1.0.9

# --- OpenAI-compatible client (talks to llama-server on :8081) ---
openai==2.45.0
distro==1.9.0
jiter==0.16.0

# --- ColBERT tokenizer + ONNX inference ---
numpy>=1.26.0
transformers==5.13.1
tokenizers==0.22.2
safetensors==0.8.0
regex
protobuf==7.35.1
flatbuffers==25.12.19
Jinja2==3.1.6
MarkupSafe==3.0.3

# --- Model downloads (used by install_linux.sh) ---
huggingface_hub==1.23.0
hf-xet==1.5.1
filelock==3.29.7
fsspec
packaging==26.2
PyYAML==6.0.3
tqdm==4.68.4

# --- Voice: Kokoro TTS + audio I/O ---
kokoro-onnx>=0.4.4
soundfile==0.14.0
sounddevice>=0.5.0
cffi==2.1.0             # soundfile/sounddevice bind via CFFI
pycparser==3.0

# --- Speech-to-Text: Parakeet TDT (parakeet.cpp) ---
# parakeet.cpp CLI (software/parakeet.cpp) runs offline Parakeet TDT ASR (models/audio/tdt-0.6b-v2-q5_k.gguf)

# --- Document parsing ---
pypdf>=5.0.0

# --- System ---
psutil
